---
import { urlFromBase } from "@/lib/utils";
interface Props {
  tag: string;
}

const base = import.meta.env.BASE_URL;
const { tag } = Astro.props;
---

<a
  href={urlFromBase(base, "tags/" + tag)}
  class="rounded-xl invert bg-white/65 dark:bg-black/60 px-2 hover:text-custom-red-inverted dark:hover:text-custom-red-inverted transition-colors duration-300 ease-in-out"
>
  #{tag}
</a>
